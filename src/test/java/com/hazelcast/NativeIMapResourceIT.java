package com.hazelcast;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeIMapResourceIT
        extends IMapResourceTest {

    // Execute the same tests but in native mode.
}